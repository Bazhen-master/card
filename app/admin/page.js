// Личный кабинет владельца сайта.
// На Этапе 2 здесь появится: загрузка новых карт, редактирование/удаление,
// изменение порядка отображения. Доступ будет защищён паролем (см. README).

export default function AdminPage() {
  return (
    <section>
      <h1 className="text-2xl font-semibold mb-4">Админка</h1>
      <p className="text-gray-500">
        Здесь появится управление каталогом карт: загрузка, редактирование,
        удаление — без необходимости обращаться к разработчику.
      </p>
    </section>
  );
}
