# План работы: Метафорические карты (пилот)

## Контекст

Открытая продажа колод метафорических карт любому желающему. Пилот без
маркетплейса третьих лиц — комиссию с чужих загруженных/сгенерированных карт
делаем отдельным этапом позже, сейчас НЕ реализуем.

Обновление 07.09.2026: «отдельный этап позже» назначен — это Этап 6 (аккаунты,
общая галерея, внутренний баланс). Пилот 1-5 остаётся работающим и без него.

## Модель данных

Использовать Supabase (Postgres + Storage) — закрывает и хранение картинок
(персистентно, в отличие от файловой системы хостинга), и базу данных.

```
cards
  id            uuid, pk
  deck_id       uuid, fk -> decks.id, nullable
  image_url     text        -- ссылка на Supabase Storage
  text          text, nullable  -- вопрос/фраза на карте
  source_type   enum('uploaded','generated')
  price         integer, nullable  -- цена поштучно, если продаётся отдельно
  created_at    timestamp

decks
  id            uuid, pk
  title         text
  description   text
  price         integer      -- цена всей колоды
  cover_image   text
  created_at    timestamp

leads                          -- для тестовой кнопки оплаты (Этап 4)
  id            uuid, pk
  deck_id       uuid, fk, nullable
  tariff        text          -- какой тариф выбрал
  contact       text          -- email/телефон
  created_at    timestamp
```

Автор/владелец карты и разделение выплат — НЕ добавлять в эту версию схемы
(зарезервировано на будущий этап). Этот будущий этап расписан ниже как Этап 6,
там же дополнение схемы: `profiles`, `purchases`, `balance_entries` и новые
поля `cards`.

Отклонение от схемы выше, внесённое на Этапе 2: в `decks` и `cards` добавлено
поле `sort_order integer not null default 0` — без него нельзя было выполнить
требование «сортировка» в админке.

## Стек

- Next.js (App Router) — уже развёрнут в этом репозитории.
- Supabase — БД + Storage + позже auth, если понадобится.
- Timeweb Cloud (App Platform) — хостинг, приложение Next.js с включённым SSR.
- Нейросеть для генерации: Kandinsky (FusionBrain API) — нужна пара ключей из
  личного кабинета заказчицы. Уточнение от 06.09.2026: сервис стал условно-платным.
  Бесплатная квота есть, но небольшая (по веб-интерфейсу — 20 изображений в месяц,
  по API называется около 100 запросов в месяц, официально подтвердить не удалось:
  fusionbrain.ai не открывается из-за рубежа). Точный тариф смотреть в кабинете.
  Следствие для Этапа 3: нужен общий месячный потолок генераций на весь сайт —
  лимита на одного посетителя при такой квоте недостаточно.

Решение от 07.09.2026: от FusionBrain (Kandinsky) отказались — дорого при
слабом качестве картинок. Новый поставщик — GenAPI (gen-api.ru), модель Flux
Schnell, 0,50 ₽ за картинку (колода из 60 карт — около 30 ₽); при
необходимости переключается на более дорогую модель одной переменной окружения.
Цена подтверждена настоящим списанием 07.09.2026, а не витриной тарифов: в
каталоге GenAPI указана базовая ставка сети, и она расходится с фактом в разы.
Оплата рублёвой картой, работает из России без VPN, один ключ на весь каталог
моделей. Код генерации переведён на сменного поставщика
(`lib/image-provider.js` + адаптеры в `lib/providers/`), чтобы следующий такой
переезд не стоил переписывания. Клиент FusionBrain оставлен как запасной.

Рассмотренные альтернативы: YandexART (~2,91 ₽ за картинку, официальный договор
и закрывающие документы, но сложнее регистрация и более «стоковый» стиль);
ProxyAPI с моделями OpenAI (качество высокое, цена для этой задачи избыточна).

Проверен nplod.ru («НейроПлод»), 07.09.2026 — НЕ подходит. Это сервис для
человека за браузером, а не для сайта: публичного API и документации для
разработчиков нет (ни раздела для разработчиков, ни упоминаний в их канале
t.me/nplod), тарифы видно только после входа в личный кабинет, внутренние
адреса вида /api/1/imageGenerator/requestGenerateImage работают по cookie
сессии — дёргать их с нашего сервера значит нарушать их правила и ловить
поломку при любой их правке. Как «сгенерировать картинки руками и загрузить
через админку» — годится, как источник для кнопки «Сгенерировать» на сайте —
нет.

Отклонение от исходного плана (05.09.2026): хостинг сменён с Render на Timeweb
Cloud. Причина — Render перестал открываться у заказчицы из России, а зарубежная
площадка не даёт на это повлиять. Заодно картинки карт больше не отдаются
браузеру напрямую с `supabase.co`: в вёрстке стоит `/api/image/<файл>`, файл из
Storage забирает наш сервер. Так у страницы остаётся ровно одна внешняя
зависимость от доступности — сам домен сайта. `render.yaml` оставлен в
репозитории как запасной вариант.

## Переменные окружения (добавить в Timeweb Cloud → Переменные окружения)

```
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
GENAPI_KEY=
ADMIN_PASSWORD=
```

Необязательные, у всех есть значение по умолчанию: `IMAGE_PROVIDER` (`genapi` или
`fusionbrain`), `GENAPI_MODEL` (`schnell`, `dev`, `pro`, `ultra`),
`GENAPI_NETWORK` (`flux`).

Ничего из этого не коммитить в git — только через .env.local локально и
переменные окружения в панели хостинга.

## Этапы

### Этап 2: Каталог готовых колод + админка — ГОТОВО
- [x] Подключить Supabase (клиент + переменные окружения).
- [x] Таблицы `cards`, `decks` в Supabase — см. `supabase/schema.sql`
      (там же `leads` на будущее и бакет Storage).
- [x] `/catalog` — сетка колод: обложка, название, цена, кнопка (пока без
      реальной оплаты — заглушка-ссылка на будущую тестовую кнопку из Этапа 4).
- [x] `/catalog/[deckId]` — страница колоды со списком карт внутри.
- [x] `/admin` — простая защита паролем (см. "Защита админки" ниже).
- [x] `/admin` — CRUD для колод и карт: загрузка картинки (в Supabase Storage),
      название/текст/цена, удаление, сортировка.

> В смете для заказчицы этот этап разбит на два пункта — «Личный кабинет»
> (700–1000 ₽) и «Каталог готовых карт» (800–1000 ₽): админка сделана и её видно
> отдельной строкой. Сумма та же, что была за этап целиком, итог пилота
> не изменился. Нумерация этапов здесь остаётся прежней, 1–5.

### Этап 3: Интеграция ИИ-генерации
- [ ] `/generate` — форма запроса к нейросети (GenAPI, Flux).
- [ ] Результат генерации сохраняется как `card` с `source_type = 'generated'`.
- [ ] Лимит генераций на сессию/пользователя (простой rate-limit, без
      полноценной системы аккаунтов на этом этапе).

> Состояние на 07.09.2026: код написан и собирается, `/generate` есть, лимит и
> сохранение карты реализованы, таблица `generations` добавлена в
> `supabase/schema.sql`, генерация переведена с FusionBrain на GenAPI (Flux).
> Галочки не проставлены, потому что сквозного прогона на настоящем API не было:
> нужен ключ GenAPI от заказчицы (с пополненным балансом) и однократный запуск
> `supabase/schema.sql` на рабочей базе. Контракт GenAPI проверен без ключа:
> адреса `POST /api/v1/networks/flux` и `GET /api/v1/request/get/{id}`
> существуют и отвечают 401 без токена, список версий модели — schnell, dev,
> pro, pro_v1.1, ultra, max. Разбор ответа написан с запасом на несколько форм
> (`result` / `output` / `full_response`, строки и объекты). Проверено на живой
> базе: подсказки при отсутствии ключей и при отсутствии таблицы.

> Прогон живым ключом 07.09.2026: ключ принят, генерация проходит полностью —
> задача, опрос статуса, скачивание картинки. Сравнение на двух запросах
> показало, что Flux Schnell не только вдвое дешевле SDXL, но и точнее держит
> инструкции: SDXL вопреки запрету пририсовывает карте декоративную рамку.
> Модель менять не стали. `flux-2-klein` отвечает 422 на пару width/height —
> если понадобится, разбираться с его форматом параметров отдельно.

> Сквозной прогон через сам сайт (локальная продакшн-сборка, живая база,
> настоящий ключ) пройден полностью: форма → server action → списание 0,50 ₽ →
> картинка в Supabase Storage → карта в базе → счётчик лимита «осталось 4 из 5»
> → показ через `/api/image/`. Описание посетителя переводится на английский
> перед генерацией (0,003 ₽), итоговый текст пишется в лог. Тестовые карты из
> базы удалены.

> Доработка 07.09.2026 по просьбе заказчицы: в форме генерации появился выбор
> формата карты — вертикальный 768×1024 (по умолчанию), горизонтальный 1024×768
> и квадрат 1024×1024; сгенерированная карта показывается в своей пропорции, а
> не обрезается под 3:4 (в каталоге колоды по-прежнему выровнены под 3:4). В
> запрос к нейросети добавлено назначение картинки — метафорическая
> ассоциативная карта для работы психолога с клиентом, один образ, допускающий
> разные прочтения. Проверено живым ключом: обе новые пропорции приходят ровно
> такими, как заказаны, надписей и рамок нет.

### Этап 4: Тестовая кнопка оплаты
- [x] Кнопка "Купить/Оформить подписку" на карточке колоды — не ведёт к
      реальному платежу.
- [x] По клику — запись в таблицу `leads` (deck_id, tariff) + модалка "Скоро
      будет доступно" с полем email/телефон.
- [x] В `/admin` — простой отчёт: сколько кликов по каждому тарифу/колоде,
      список контактов.

> Сделано 07.09.2026, после Этапа 6. Вместо модального окна — панель, которая
> появляется на самой странице: на сайте нет ни строчки клиентского JS, и
> заводить его ради одного окна не стали; работает одинаково с любым браузером
> и без скриптов. Тарифа два: покупка колоды и подписка на все колоды;
> подделанное значение из формы откатывается к покупке колоды.
>
> Нажатие пишется в `leads` сразу, до контакта: клик — уже сигнал, а контакт
> оставляют не все, и разница между этими числами сама по себе полезна.
> Дописать контакт можно только в свою свежую заявку (не старше часа и ещё
> пустую) — номер заявки виден в адресной строке, и чужую перезаписать нельзя.
>
> Прогон на живой базе: нажатие создало заявку и показало панель; пустой
> контакт отклонён; контакт сохранился; повторная запись в ту же заявку
> отклонена; подделанный тариф записался как покупка колоды; отчёт в
> `/admin/leads` показал 3 нажатия, разбивку по тарифам и колодам и один
> контакт. Тестовые заявки удалены.

### Этап 5: Тестирование и запуск
- [ ] Прогнать все сценарии (каталог → колода → карта; генерация; клик по
      тестовой кнопке; вход в админку и CRUD).
- [ ] Финальный деплой на Timeweb Cloud, проверка доступности без VPN.

### Этап 6: Аккаунты, общая галерея и внутренний баланс

Заказан 07.09.2026. Пилот (Этапы 1-5) остаётся как есть и продолжает работать
без аккаунтов: посетитель без входа по-прежнему смотрит каталог и генерирует
карты по cookie-лимиту. Всё ниже — надстройка над ним.

**Что должно получиться словами заказчицы.** Посетитель заводит учётную запись,
видит историю своих генераций, может выставить свою карту в общую галерею с
ценой от 3 ₽ и может купить чужую. До покупки карта в галерее показывается с
полупрозрачным логотипом на всю картинку, после покупки — чистой.

#### Принятые решения (07.09.2026)

| Развилка | Решение | Почему |
| --- | --- | --- |
| Вход | E-mail + пароль, Supabase Auth, подтверждение почты пока выключено | Работает сразу, не требует ни своей рассылки писем, ни модерации на стороне VK/Telegram. Пароли хранит и проверяет Supabase, свой велосипед не пишем |
| Деньги | Внутренний баланс: пополнение крупной суммой, покупки списываются с него | Платёжная система берёт за операцию процент плюс фиксированную сумму — на прямом платеже в 3 ₽ площадка уходит в минус. Баланс — единственный способ оставить цену в 3 ₽ |
| Автору | Внутренние баллы без вывода: тратятся на генерации и покупки | Не маркетплейс: не нужны агентские договоры с каждым автором, чеки за них и проверка самозанятости. Деньги площадка получает вперёд, при пополнении |
| Галерея | Публикация после проверки в /admin | За чужие фотографии и запрещённый контент отвечает владелец площадки, а не автор |

Появится ещё одна переменная окружения — публичный (anon) ключ Supabase из того
же кабинета, что и service-role. Он нужен для проверки пароля при входе; в
отличие от service-role его не страшно отдавать браузеру, доступа к таблицам он
не даёт (RLS включён, политик нет). Данные, как и сейчас, читает и пишет только
серверный код.

> Из этого следует важное: пока баллы нельзя вывести, экономика замкнутая.
> Посетитель платит деньги площадке при пополнении, а внутри они ходят между
> авторами. Это осознанное ограничение пилота, а не недоделка, и его надо прямо
> написать в правилах публикации — иначе автор будет ждать выплату на карту.

#### Модель данных (дополнение к схеме выше)

```
profiles                      -- профиль поверх auth.users Supabase
  id            uuid, pk, fk -> auth.users.id
  display_name  text          -- имя автора под картой
  is_blocked    boolean default false
  created_at    timestamp

cards (добавить поля)
  owner_id      uuid, fk -> profiles.id, nullable   -- null у карт админки
  status        enum('private','pending','listed','rejected')
  price         integer       -- копейки, минимум 300 (3 ₽)
  preview_url   text          -- превью с водяным знаком, публичное
  reject_reason text, nullable

purchases
  id            uuid, pk
  buyer_id      uuid, fk -> profiles.id
  card_id       uuid, fk -> cards.id
  price         integer       -- копейки, цена на момент покупки
  created_at    timestamp
  unique (buyer_id, card_id)  -- дважды одно и то же не продаём

balance_entries               -- журнал движения баланса, источник правды
  id            uuid, pk
  profile_id    uuid, fk -> profiles.id
  delta         integer       -- копейки, со знаком
  kind          enum('topup','purchase','sale','generation','refund','admin')
  card_id       uuid, nullable
  comment       text, nullable
  created_at    timestamp
```

Баланс нигде не хранится отдельным числом — он равен сумме `delta` по журналу.
Так расхождение «на счету 12 ₽, а по операциям 9 ₽» становится невозможным, и
любой спор с автором разбирается по строкам журнала. Если суммирование окажется
медленным (это тысячи строк, не миллионы — вряд ли), кэш добавим отдельно.

**Деньги везде в копейках.** Существующие `decks.price` и `cards.price` хранят
рубли, их надо умножить на 100 разовой миграцией: комиссия площадки с карты за
3 ₽ в целых рублях не считается. В формах админки и на витрине цена остаётся в
рублях, копейки — только внутри базы.

#### Как работает водяной знак

Единственный надёжный способ — накладывать знак на сервере, отдельным файлом.
Полупрозрачный логотип, наложенный в вёрстке поверх картинки, снимается в
браузере за десять секунд, поэтому так делать нельзя.

1. Оригинал переезжает в **приватный** бакет Storage. Публичной ссылки у него
   нет вообще.
2. При публикации сервер делает превью: уменьшает картинку по длинной стороне
   (~700 px) и накладывает полупрозрачный логотип на всю площадь. Превью лежит
   в публичном бакете — его и видят в галерее.
3. Оригинал отдаётся через `/api/image/` только владельцу карты, покупателю и
   админу. Проверка — на сервере, по сессии, а не по ссылке.
4. Кнопка «Скачать оригинал» появляется на карточке только после покупки.

Инструмент — библиотека `sharp` (composite + resize). Риск: она собирается под
конкретную платформу, и на Timeweb App Platform это надо проверить в первый же
день работы над этапом; если не встанет — запасной вариант `@napi-rs/canvas`.

Логотип пока не согласован, поэтому первая версия знака — текстовая: название
сайта полупрозрачной надписью, повторённой по диагонали через всю карту. Способ
наложения от этого не меняется, меняется только то, что накладывается, так что
подстановка логотипа позже переделкой не будет.

#### Экраны

- `/register`, `/login`, выход — форма e-mail + пароль, согласие на обработку
  персональных данных галочкой при регистрации.
- `/account` — баланс и история операций, мои генерации, мои публикации с их
  статусом, мои покупки.
- `/gallery` — общая галерея: превью с водяным знаком, имя автора, цена,
  кнопка «Купить за N ₽».
- `/gallery/[cardId]` — одна карта крупно, описание, покупка, после покупки —
  скачивание оригинала.
- `/generate` — под своей картой кнопка «Выставить в галерею» с полем цены
  (минимум 3 ₽); история генераций у вошедшего берётся из аккаунта, а не из
  cookie.
- `/admin/moderation` — очередь на проверку: карта, автор, цена, кнопки
  «Опубликовать» и «Отклонить» с причиной.
- `/admin/users` — список пользователей, баланс, ручное начисление (пока
  платёжной системы нет, пополнение делается руками) и блокировка.

#### Порядок работы (сдаётся кусками, каждый кусок полезен сам по себе)

- [x] **6.1 Аккаунты.** Supabase Auth, регистрация/вход/выход, `profiles`,
      `/account`, привязка генераций к аккаунту. При первом входе карты,
      сгенерированные под текущей cookie, переносятся в аккаунт — иначе
      посетитель потеряет свою историю ровно в момент регистрации.
- [x] **6.2 Водяной знак.** Приватный бакет для оригиналов, генерация превью,
      проверка прав в `/api/original/`, кнопка скачивания у владельца.
      Знак пока текстовый — логотип ещё согласовывается, см. открытые вопросы.
- [x] **6.3 Галерея и модерация.** Публикация карты с ценой, статусы,
      `/gallery`, очередь в админке. Писем автору не шлём — статус виден
      в `/account`.
- [x] **6.4 Баланс и покупки.** Журнал операций, покупка карты за баллы,
      комиссия площадки (переменная окружения, 30 % по согласованию),
      начисление автору, ручное пополнение из админки.
- [x] **6.5 Платные генерации сверх лимита.** Первые 5 в сутки бесплатно, как
      сейчас; дальше — 2 ₽ с баланса. Это то, ради чего баллы вообще нужны
      автору, и это же окупает генерацию для площадки.

> Состояние Этапа 6 на 07.09.2026: все пять кусков сделаны, прогнаны на живой
> базе и запушены. Не закрыты вещи, которые зависят не от кода: логотип для
> водяного знака (пока текстовый), юридические тексты и уведомление
> Роскомнадзора, реальное пополнение баланса (Этап 7).

#### Открытые вопросы и ответы на них (07.09.2026)

- **Комиссия площадки с продажи — 30 %.** Согласовано. Автору с карты за 3 ₽
  достаётся 2,10 ₽ на внутренний баланс. Значение хранится в переменной
  окружения, менять его можно без правки кода.
- **Генерация сверх бесплатного лимита — 2 ₽.** Согласовано. Первые 5 в сутки
  бесплатно, как сейчас, дальше списание с баланса. Себестоимость 0,50 ₽, то
  есть генерация окупает и себя, и часть выплаченных авторам баллов.
- **Логотип для водяного знака — отложен, требует согласования.** Не блокирует
  работу: на Этапе 6.2 знак делается текстовым (полупрозрачная надпись с
  названием сайта, повторённая по всей карте) плюс пониженное разрешение
  превью. Когда логотип появится, он подставляется вместо надписи — это замена
  одного файла и пары строк, переделывать этап не придётся.
- **Правила публикации, оферта, политика обработки персональных данных** —
  принято к работе заказчицей. Нужны до запуска Этапа 6 в открытом доступе:
  как только на сайте появляются регистрация и чужие деньги, эти документы
  обязательны, а обработка персональных данных требует уведомления
  Роскомнадзора. Вопрос к юристу, не к коду.

> Сделано 07.09.2026 (6.1). Отклонения от замысла, проверенные живым ключом:
> отдельный публичный (anon) ключ Supabase не понадобился — служебный ключ
> и создаёт запись с уже подтверждённой почтой, и проверяет пароль при входе,
> так что новых переменных окружения этап не добавил. Сессия своя, таблицей
> `sessions`, а не токеном Supabase: строку можно удалить и закрыть доступ
> немедленно. Минимальную длину пароля (8 символов) проверяем сами — у Supabase
> своей нет, пароль «123» он принимает. Суточный лимит генераций у вошедшего
> считается по аккаунту, а не по cookie.
>
> Сделано 07.09.2026 (6.5) — Этап 6 закрыт целиком. Сверх пяти бесплатных
> генераций вошедший рисует по 2 ₽ с баланса (`GENERATION_PRICE`), гостю
> показывается приглашение войти. Деньги списываются до генерации, при осечке
> строка списания удаляется. Оплаченные генерации вычитаются из суточного
> счёта, поэтому завтрашние бесплатные они не съедают; отдельной колонки в базе
> для этого не понадобилось — хватило строки в журнале баланса, и владелице
> сайта не пришлось идти в SQL-редактор ещё раз.
>
> Прогон на живой базе: бесплатный лимит исчерпан поддельными записями журнала
> (чтобы не жечь оплаченные генерации), кнопка стала «Сгенерировать за 2 ₽»,
> настоящая платная генерация списала 200 копеек и привязала списание к карте,
> баланс 10 → 8 ₽; при балансе 1 ₽ генерация отклонена без списания; гостю с
> исчерпанным лимитом показано приглашение войти. Тестовые данные удалены.
>
> Сделано 07.09.2026 (6.4). Баланс — сумма журнала `balance_entries`,
> отдельного «остатка» нет. Покупка списывает цену у покупателя и начисляет
> автору долю за вычетом комиссии 30 %; остаток при делении идёт автору
> (`splitPrice` в `lib/balance.js`). Купленная карта открывается без знака —
> `/api/original` пускает покупателя. Пополнение и блокировка — в
> `/admin/users`; блокировка гасит и сессии, и вход.
>
> Настоящей транзакции нет: PostgREST не даёт обернуть несколько запросов в
> BEGIN/COMMIT без своей функции в Postgres. Порядок выбран так, чтобы осечка
> откатывалась: покупка (защищена уникальным индексом) → списание →
> перепроверка баланса → начисление автору. Уйти в минус нельзя. Единственная
> дыра: если упадёт последнее начисление автору, деньги списаны, а автору не
> пришли — в лог уходит громкая строка, начисляется руками. Закрывать это
> функцией в Postgres имеет смысл, когда деньги станут настоящими (Этап 7).
>
> Прогон на живой базе: покупка без денег отклонена; начисление 100 ₽ из
> админки появилось в кабинете как «Пополнение»; покупка за 3,5 ₽ дала
> −350 копеек покупателю и +245 автору (комиссия 105), баланс 96,5 ₽; повторная
> покупка и покупка своей же карты отклонены; покупателю оригинал отдаётся
> (200), чужому — 403; блокировка выкинула из кабинета и не пустила обратно.
> Тестовые данные удалены.
>
> Сделано 07.09.2026 (6.3). Путь карты: private → pending → listed либо
> rejected с причиной. Кабинет показывает статус и даёт выставить, отозвать и
> убрать карту; там же имя автора для подписи под картами (почта наружу не
> выходит). Галерея показывает только опубликованное и только превью со знаком;
> страницы неопубликованной карты для чужого не существует. Очередь модерации —
> в `/admin/moderation`, счётчик очереди виден на главной странице админки,
> отказ без причины не принимается.
>
> Цены переведены в копейки, как и было записано в плане. Заказчица спросила,
> не проще ли считать в десятых долях рубля: проще не становится — код тот же,
> разница только в множителе, зато любая сумма с копейками (пополнение,
> возврат, комиссия банка) теряла бы остаток. Оставили копейки, но разрешили
> дробные цены с шагом в десятую: 1,4 ₽ — обычная цена, показывается без лишних
> нулей. Остаток при делении комиссии на 6.4 отдавать автору.
>
> Прогон на живой базе: цена ниже 3 ₽ отклонена; публикация за 3,5 ₽ ушла в
> очередь и в галерее не появилась; админка показала «на проверке: 1»;
> одобрение вывело карту в галерею, на главную и на её страницу; чужому
> оригинал — 403, показывается превью со знаком; имя автора появилось под
> картой; отзыв убрал карту с витрины; отказ без причины отклонён, с причиной —
> причина видна автору в кабинете. Тестовые данные удалены, ничьих файлов в
> обоих бакетах нет.
>
> Ещё грабли: в JavaScript `String.replace` понимает `$$` в тексте замены как
> один доллар — в SQL это разделитель блока, и в схему попали битые `do $`.
> Ловится только чтением файла после правки.
>
> Сделано 07.09.2026 (6.2). Оригиналы переехали в закрытый бакет
> `cards-private`, публичных ссылок у них нет; в `cards.image_url` для них
> хранится пометка `private://<файл>`, а рядом в `preview_url` — превью 700 px
> с водяным знаком. Права на оригинал проверяет `/api/original/<id>`: владелец,
> гость со своей сессией, админ, позже покупатель. Знак — готовая картинка
> `public/watermark.png`, повторённая плиткой: рисовать текст на сервере нельзя,
> в контейнере может не быть шрифтов, и надпись вышла бы пустотой. Замена
> логотипа = замена файла. Осечка `sharp` не отменяет уже оплаченную генерацию:
> карта сохраняется без превью, в галерею её не пустят.
>
> Грабли, найденные сквозным прогоном: после `signInWithPassword` клиент
> Supabase запоминает вошедшего и начинает ходить в базу его токеном вместо
> служебного ключа — а для обычного пользователя RLS закрыта наглухо, политик у
> таблиц нет, и вход падал на чтении профиля. Поэтому пароль проверяется одним
> экземпляром клиента, а с таблицами работает другой. На сборке это не видно,
> ловится только живым прогоном.
>
> Прогон 07.09.2026 на живой базе, через настоящие формы (они работают и без
> JavaScript, поэтому нажимаются curl'ом): короткий пароль отклонён, пароли не
> совпадают — отклонено, без согласия — отклонено, повторная регистрация того
> же адреса — отклонена. Регистрация переносит карты гостя в аккаунт; выход
> удаляет строку сессии; `/account` без входа уводит на `/login`; неверный
> пароль отклоняется; после входа история на месте. Генерация из-под аккаунта
> (0,50 ₽) сразу записала владельца, лимит пошёл по аккаунту: 5 → 4 → 3.
> Тестовые записи, карты и файл в Storage удалены, ничьих файлов не осталось.
>
> Вне очереди по просьбе заказчицы: на главной странице появилась карусель со
> всеми картами из колод каталога. Карты посетителей туда не идут до галереи
> и модерации (6.3).

#### Оценка

Ориентировочно: 6.1 — 1,5-2 дня, 6.2 — 1-1,5 дня, 6.3 — 1,5-2 дня,
6.4 — 1,5-2 дня, 6.5 — полдня. Итого около недели работы. Цифры уточняются
после ответа по комиссии и логотипу.

### Этап 7: Реальные деньги (следующий за Этапом 6)

Пополнение баланса через ЮKassa: платёжная страница, возврат на сайт, webhook
о зачислении, повторная проверка суммы на сервере. Требует от заказчицы статуса
ИП или самозанятого, договора с ЮKassa, онлайн-кассы или её облачного аналога
для чеков по 54-ФЗ. Пока этого нет, баланс пополняется вручную из админки — на
пилоте этого достаточно.

## Защита админки

Без БД пользователей: пароль хранится в `ADMIN_PASSWORD` (переменная
окружения), middleware Next.js проверяет cookie/сессию перед рендером
`/admin/*`, при отсутствии — форма логина. Пароль никогда не хранится в коде
или в git.

## Отложено на будущий этап (не делать сейчас)

- Выплаты авторам живыми деньгами (агентская схема, Безопасная сделка или
  сплитование платежей в ЮKassa, проверка самозанятости, чеки за автора).
  На Этапе 6 вместо этого — внутренние баллы без вывода.
- Автоматическая проверка контента (распознавание запрещённого) — на Этапе 6
  модерация ручная, глазами.
- Восстановление пароля письмом и подтверждение e-mail — нужен свой почтовый
  сервис, у Supabase бесплатный лимит писем около 2-3 в час.
- Вход через Telegram, VK ID, Яндекс ID.
- Продажа целых колод от пользователей — на Этапе 6 продаются только отдельные
  карты.
