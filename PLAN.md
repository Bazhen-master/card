# План работы: Метафорические карты (пилот)

## Контекст

Открытая продажа колод метафорических карт любому желающему. Пилот без
маркетплейса третьих лиц — комиссию с чужих загруженных/сгенерированных карт
делаем отдельным этапом позже, сейчас НЕ реализуем.

## Модель данных

Использовать Supabase (Postgres + Storage) — закрывает и хранение картинок
(персистентно, в отличие от файловой системы хостинга), и базу данных.

```
cards
  id            uuid, pk
  deck_id       uuid, fk -> decks.id, nullable
  image_url     text        -- ссылка на Supabase Storage
  text          text, nullable  -- вопрос/фраза на карте
  source_type   enum('uploaded','generated')
  price         integer, nullable  -- цена поштучно, если продаётся отдельно
  created_at    timestamp

decks
  id            uuid, pk
  title         text
  description   text
  price         integer      -- цена всей колоды
  cover_image   text
  created_at    timestamp

leads                          -- для тестовой кнопки оплаты (Этап 4)
  id            uuid, pk
  deck_id       uuid, fk, nullable
  tariff        text          -- какой тариф выбрал
  contact       text          -- email/телефон
  created_at    timestamp
```

Автор/владелец карты и разделение выплат — НЕ добавлять в эту версию схемы
(зарезервировано на будущий этап).

Отклонение от схемы выше, внесённое на Этапе 2: в `decks` и `cards` добавлено
поле `sort_order integer not null default 0` — без него нельзя было выполнить
требование «сортировка» в админке.

## Стек

- Next.js (App Router) — уже развёрнут в этом репозитории.
- Supabase — БД + Storage + позже auth, если понадобится.
- Timeweb Cloud (App Platform) — хостинг, приложение Next.js с включённым SSR.
- Нейросеть для генерации: Kandinsky (FusionBrain API) — нужна пара ключей из
  личного кабинета заказчицы. Уточнение от 06.09.2026: сервис стал условно-платным.
  Бесплатная квота есть, но небольшая (по веб-интерфейсу — 20 изображений в месяц,
  по API называется около 100 запросов в месяц, официально подтвердить не удалось:
  fusionbrain.ai не открывается из-за рубежа). Точный тариф смотреть в кабинете.
  Следствие для Этапа 3: нужен общий месячный потолок генераций на весь сайт —
  лимита на одного посетителя при такой квоте недостаточно.

Решение от 07.09.2026: от FusionBrain (Kandinsky) отказались — дорого при
слабом качестве картинок. Новый поставщик — GenAPI (gen-api.ru), модель Flux
Schnell, 0,50 ₽ за картинку (колода из 60 карт — около 30 ₽); при
необходимости переключается на более дорогую модель одной переменной окружения.
Цена подтверждена настоящим списанием 07.09.2026, а не витриной тарифов: в
каталоге GenAPI указана базовая ставка сети, и она расходится с фактом в разы.
Оплата рублёвой картой, работает из России без VPN, один ключ на весь каталог
моделей. Код генерации переведён на сменного поставщика
(`lib/image-provider.js` + адаптеры в `lib/providers/`), чтобы следующий такой
переезд не стоил переписывания. Клиент FusionBrain оставлен как запасной.

Рассмотренные альтернативы: YandexART (~2,91 ₽ за картинку, официальный договор
и закрывающие документы, но сложнее регистрация и более «стоковый» стиль);
ProxyAPI с моделями OpenAI (качество высокое, цена для этой задачи избыточна).

Проверен nplod.ru («НейроПлод»), 07.09.2026 — НЕ подходит. Это сервис для
человека за браузером, а не для сайта: публичного API и документации для
разработчиков нет (ни раздела для разработчиков, ни упоминаний в их канале
t.me/nplod), тарифы видно только после входа в личный кабинет, внутренние
адреса вида /api/1/imageGenerator/requestGenerateImage работают по cookie
сессии — дёргать их с нашего сервера значит нарушать их правила и ловить
поломку при любой их правке. Как «сгенерировать картинки руками и загрузить
через админку» — годится, как источник для кнопки «Сгенерировать» на сайте —
нет.

Отклонение от исходного плана (05.09.2026): хостинг сменён с Render на Timeweb
Cloud. Причина — Render перестал открываться у заказчицы из России, а зарубежная
площадка не даёт на это повлиять. Заодно картинки карт больше не отдаются
браузеру напрямую с `supabase.co`: в вёрстке стоит `/api/image/<файл>`, файл из
Storage забирает наш сервер. Так у страницы остаётся ровно одна внешняя
зависимость от доступности — сам домен сайта. `render.yaml` оставлен в
репозитории как запасной вариант.

## Переменные окружения (добавить в Timeweb Cloud → Переменные окружения)

```
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
GENAPI_KEY=
ADMIN_PASSWORD=
```

Необязательные, у всех есть значение по умолчанию: `IMAGE_PROVIDER` (`genapi` или
`fusionbrain`), `GENAPI_MODEL` (`schnell`, `dev`, `pro`, `ultra`),
`GENAPI_NETWORK` (`flux`).

Ничего из этого не коммитить в git — только через .env.local локально и
переменные окружения в панели хостинга.

## Этапы

### Этап 2: Каталог готовых колод + админка — ГОТОВО
- [x] Подключить Supabase (клиент + переменные окружения).
- [x] Таблицы `cards`, `decks` в Supabase — см. `supabase/schema.sql`
      (там же `leads` на будущее и бакет Storage).
- [x] `/catalog` — сетка колод: обложка, название, цена, кнопка (пока без
      реальной оплаты — заглушка-ссылка на будущую тестовую кнопку из Этапа 4).
- [x] `/catalog/[deckId]` — страница колоды со списком карт внутри.
- [x] `/admin` — простая защита паролем (см. "Защита админки" ниже).
- [x] `/admin` — CRUD для колод и карт: загрузка картинки (в Supabase Storage),
      название/текст/цена, удаление, сортировка.

> В смете для заказчицы этот этап разбит на два пункта — «Личный кабинет»
> (700–1000 ₽) и «Каталог готовых карт» (800–1000 ₽): админка сделана и её видно
> отдельной строкой. Сумма та же, что была за этап целиком, итог пилота
> не изменился. Нумерация этапов здесь остаётся прежней, 1–5.

### Этап 3: Интеграция ИИ-генерации
- [ ] `/generate` — форма запроса к нейросети (GenAPI, Flux).
- [ ] Результат генерации сохраняется как `card` с `source_type = 'generated'`.
- [ ] Лимит генераций на сессию/пользователя (простой rate-limit, без
      полноценной системы аккаунтов на этом этапе).

> Состояние на 07.09.2026: код написан и собирается, `/generate` есть, лимит и
> сохранение карты реализованы, таблица `generations` добавлена в
> `supabase/schema.sql`, генерация переведена с FusionBrain на GenAPI (Flux).
> Галочки не проставлены, потому что сквозного прогона на настоящем API не было:
> нужен ключ GenAPI от заказчицы (с пополненным балансом) и однократный запуск
> `supabase/schema.sql` на рабочей базе. Контракт GenAPI проверен без ключа:
> адреса `POST /api/v1/networks/flux` и `GET /api/v1/request/get/{id}`
> существуют и отвечают 401 без токена, список версий модели — schnell, dev,
> pro, pro_v1.1, ultra, max. Разбор ответа написан с запасом на несколько форм
> (`result` / `output` / `full_response`, строки и объекты). Проверено на живой
> базе: подсказки при отсутствии ключей и при отсутствии таблицы.

> Прогон живым ключом 07.09.2026: ключ принят, генерация проходит полностью —
> задача, опрос статуса, скачивание картинки. Сравнение на двух запросах
> показало, что Flux Schnell не только вдвое дешевле SDXL, но и точнее держит
> инструкции: SDXL вопреки запрету пририсовывает карте декоративную рамку.
> Модель менять не стали. `flux-2-klein` отвечает 422 на пару width/height —
> если понадобится, разбираться с его форматом параметров отдельно.

> Сквозной прогон через сам сайт (локальная продакшн-сборка, живая база,
> настоящий ключ) пройден полностью: форма → server action → списание 0,50 ₽ →
> картинка в Supabase Storage → карта в базе → счётчик лимита «осталось 4 из 5»
> → показ через `/api/image/`. Описание посетителя переводится на английский
> перед генерацией (0,003 ₽), итоговый текст пишется в лог. Тестовые карты из
> базы удалены.

> Доработка 07.09.2026 по просьбе заказчицы: в форме генерации появился выбор
> формата карты — вертикальный 768×1024 (по умолчанию), горизонтальный 1024×768
> и квадрат 1024×1024; сгенерированная карта показывается в своей пропорции, а
> не обрезается под 3:4 (в каталоге колоды по-прежнему выровнены под 3:4). В
> запрос к нейросети добавлено назначение картинки — метафорическая
> ассоциативная карта для работы психолога с клиентом, один образ, допускающий
> разные прочтения. Проверено живым ключом: обе новые пропорции приходят ровно
> такими, как заказаны, надписей и рамок нет.

### Этап 4: Тестовая кнопка оплаты
- [ ] Кнопка "Купить/Оформить подписку" на карточке колоды — не ведёт к
      реальному платежу.
- [ ] По клику — запись в таблицу `leads` (deck_id, tariff) + модалка "Скоро
      будет доступно" с полем email/телефон.
- [ ] В `/admin` — простой отчёт: сколько кликов по каждому тарифу/колоде,
      список контактов.

### Этап 5: Тестирование и запуск
- [ ] Прогнать все сценарии (каталог → колода → карта; генерация; клик по
      тестовой кнопке; вход в админку и CRUD).
- [ ] Финальный деплой на Timeweb Cloud, проверка доступности без VPN.

## Защита админки

Без БД пользователей: пароль хранится в `ADMIN_PASSWORD` (переменная
окружения), middleware Next.js проверяет cookie/сессию перед рендером
`/admin/*`, при отсутствии — форма логина. Пароль никогда не хранится в коде
или в git.

## Отложено на будущий этап (не делать сейчас)

- Аккаунты пользователей для продажи карт/колод третьих лиц.
- Комиссия площадки с продаж чужого контента.
- Выплаты авторам (Безопасная сделка / сплитование платежей в ЮKassa).
- Модерация загружаемого контента.
- Реальная интеграция платёжной системы (придёт на смену Этапу 4, когда пилот
  подтвердит спрос).
